// backend/prisma/schema.prisma
// Add these changes to your existing schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Update your existing Player model to include importSessionId
model Player {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Basic player info
  name     String
  position String
  team     String?
  
  // Rankings and projections
  rank            Int?
  customRank      Int?
  projectedPoints Float?
  vorp            Float?
  adp             Float?
  byeWeek         Int?
  
  // Sleeper integration
  sleeperId   String?  @unique
  dataSource  String   @default("sleeper") // "sleeper", "excel", "manual"
  lastSyncAt  DateTime?
  
  // Draft status
  isDrafted Boolean @default(false)
  
  // Relationships
  tierId String?
  tier   Tier?   @relation(fields: [tierId], references: [id])
  
  playerTags PlayerTag[]
  notes      Note[]
  
  // Additional fields
  aliases                String[]
  depthChartPosition     String?
  depthChartOrder        Int?
  
  // NEW FIELD FOR IMPORT TRACKING
  importSessionId String?  // Track which import session created/modified this player

  @@map("players")
}

// NEW MODEL: Import Session tracking
model ImportSession {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  summary    Json     // Summary statistics of the import
  changes    Json     // Detailed list of changes made
  rolledBack Boolean  @default(false)
  
  @@map("import_sessions")
}

model Tier {
  id    String @id @default(uuid())
  name  String @unique
  color String
  order Int    @unique

  players Player[]

  @@map("tiers")
}

model Tag {
  id    String @id @default(uuid())
  name  String @unique
  color String

  playerTags PlayerTag[]

  @@map("tags")
}

model PlayerTag {
  id       String @id @default(uuid())
  playerId String
  tagId    String

  player Player @relation(fields: [playerId], references: [id], onDelete: Cascade)
  tag    Tag    @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@unique([playerId, tagId])
  @@map("player_tags")
}

model Note {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  content  String
  color    String   @default("#yellow")
  playerId String

  player Player @relation(fields: [playerId], references: [id], onDelete: Cascade)

  @@map("notes")
}